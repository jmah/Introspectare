{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                revert = id; 
                save = id; 
                showConstitutions = id; 
                showDays = id; 
                showHideInspector = id; 
                showInspector = id; 
                synchronize = id; 
            }; 
            CLASS = INTAppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {syncProgressIndicator = NSProgressIndicator; syncProgressPanel = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}